version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm install
        - cd nextjs-app
    build:
      commands:
        - export NEXT_PUBLIC_DISABLE_ESLINT=true
        - npm run build
        - echo "✅ Next.js build completed"
  artifacts:
    baseDirectory: nextjs-app/out
    files:
      - '**/*'
  cache:
    paths:
      - nextjs-app/node_modules/**/*